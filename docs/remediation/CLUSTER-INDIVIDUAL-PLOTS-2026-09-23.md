# Individual cluster plots — 2026-09-23 UTC

The earlier large-cluster containment replaced nearby map events with grouped markers and sampled sequence-chart events. That made individual event positions disappear from the two plots even though the detail list retained every member. The local code candidate `9864b597d01c38d6e9800c8419e03f8488797fdc`, based on deployed `f381899fd1fb56c8753c24342d8ebb6d9520c53b`, restores one point per valid member on both plots.

The cluster map draws its members with one shared Leaflet canvas renderer. The latest event keeps its highlighted marker. A map click selects all member centers within ten screen pixels of the clicked event; this includes coincident points hidden beneath the highlighted marker. The sequence chart draws every valid time/magnitude point and the complete eligible line. Clicking a circle selects all overlapping drawn circles. The chart has one keyboard stop: arrow keys choose a point, a live status describes it, and Enter or Space selects all its overlaps. Plot selection filters the existing 50-row paginated list for inspection; clearing restores the full list. Other earthquake maps keep their previous marker and popup behavior.

## Verification

- Regressions cover 1,217 individually plotted map and chart members, 1,217 coincident chart selections, 51 coincident map events beneath the latest highlight, paginated disambiguation and clearing, chart keyboard access, mounted-map bounds fitting, and regional map rerenders. The final full suite passed **1,620 tests across 127 files, three skipped**. Changed-file ESLint, `git diff --check`, Vite build, and production packaging dry run passed. Repository-wide lint still has 40 pre-existing errors outside the changed files.
- Independent review found no remaining release blocker. It caught overlap and excess keyboard stops before preview. The 1,217-event browser check then exposed a map fit timing bug, and a final review caught a regional map viewport reset. Both were fixed and the regional map regression was added.
- The immediately preceding `f381899` build contained 26 JS/CSS files. Every file matched the frozen source by size and SHA-256. Staging these with the already retained 167 paths produced an exact **190-path, 30,926,571-byte** manifest, with **23 new paths**. The isolated preview R2 bucket was create-only archived and read back against all 190 source hashes.
- Dedicated preview Worker version `21a202cf-d8e4-45e5-88d3-de1b54584175` reports the exact code revision `9864b597d01c38d6e9800c8419e03f8488797fdc` with `status: ok`. It uses separate preview D1/KV/R2/Queue resources and no cron. Its D1 had no pending migrations; only synthetic fixtures were refreshed. The final exact-code GET smoke passed **243 checks and 211 current or retained assets**. A previous smoke run exposed its old 200-asset cap, which was corrected with a regression in the second commit.
- Chrome on the direct three-event synthetic cluster route rendered all chart circles, one map canvas and the highlighted marker, and retained all event rows. Pointer chart selection, arrow-key/Enter selection, and map selection of two nearby events each filtered the detail list. A separate 1,217-event preview D1 fixture returned all members through the API and rendered **1,217 SVG chart points, one map canvas and 50 paginated list rows**. After the mounted-map fix, tiles fitted at zoom 15 rather than the previous zoom 8, and clicking the highlighted map marker selected four nearby events rather than all 1,217. The chart had one keyboard stop; its keyboard selection grouped eight overlapping points. Browser error logs were empty. The temporary 1,217-event rows were removed from preview D1 afterward and a remote count confirmed zero remaining test events and clusters. No real-browser frame-time or Core Web Vitals claim is made.

## Production gate and recovery

Production promotion completed. The 190-path manifest was create-only archived and byte-verified in production R2, with 23 new objects totaling 2,517,620 bytes. The exact reviewed code commit was promoted to `main`. The initial Cloudflare build deployed it but its smoke gate failed on an intermittent week-feed 503 that had also occurred on the preceding version. A guarded retry of the same code passed the formal gate. Both production hosts then reported revision `9864b597d01c38d6e9800c8419e03f8488797fdc` and Worker version `aa9f05dd-0b40-4b76-b828-458ca7a91207`; the independent smoke passed 243 GET checks and 211 asset checks on each host. A live 47-member cluster showed 47 chart points, one map canvas, and keyboard selection filtering the event list. Long-span x-axis labels were crowded in that view and are a follow-up. Full evidence and build links are in `RELEASE-LEDGER.md`. No production D1 migration or synthetic data occurred.

The immediately preceding code revision `f381899` is a compatible code recovery candidate after fresh identity/configuration readback. Its retained assets and cluster data must remain available across a rollback. A code rollback does not restore D1/R2 data; this release itself has no schema or data change.

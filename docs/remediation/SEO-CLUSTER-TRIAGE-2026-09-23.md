# SEO and clustering triage — 2026-09-23 UTC

## Priority and observed evidence

1. **Monitor the released sitemap/canonical correction.** Search Console showed 1,892 indexed pages and no manual action or security issue at triage, so the site was not delisted. Its September 17 Page Indexing report listed 445 excluded URLs, including 393 “Crawled — currently not indexed”; a sampled URL in that group was indexed in URL Inspection, so the aggregate report was not a current per-URL verdict. The corrected `/sitemap-index.xml` has since been submitted and accepted. Indexing impact remains unmeasured.
2. **Fence new database writes by USGS source revision.** Production migration `0019_add_earthquake_source_revision.sql` was applied after a verified D1 export and local restore rehearsal; the nullable `source_updated_at_ms` column is present. The guarded writer code remains undeployed. Historical rows remain unresolved.
3. **Reduce repeated cluster publication and large-route work.** The ten-minute scheduled cluster path still needs a bounded no-change guard and observation. The previous cluster sitemap could perform one USGS detail request per selected row, up to 500 per sitemap request. The released sitemap emits stored canonical slugs with no upstream detail fetch. Cloudflare's triage aggregate showed about 35,190 Worker invocations, zero Worker errors, and about 2,000 USGS subrequests across all routes; it did not attribute those calls to the sitemap, so a production storm or traffic reduction has not been measured.
4. **Continue operational cleanup** after the integrity and delivery work: deferred R2 objects, predecessor-asset retention automation, dependency/test debt, and the unobserved daily scheduled job.

## Code and initial preview history

- Branch: `codex/seo-cluster-triage`; code commits `9400123` and `cb440ff`. The sitemap index and quake pages share one eligibility rule; quake entries use canonical `/quake/id/<id>` URLs. Static sitemap entries cover all Learn pages. Feed query pages declare their selected canonical. Unverified sitemap modification dates are omitted. Cluster sitemap entries use stored slugs, make no USGS detail calls, and return an uncached error on D1 failure.
- Dedicated preview Worker `earthquake-reconcile-preview`: version `919042b3-4cd5-4cc2-adc2-881f3ef17917` from the isolated code-only checkout. Preview D1/KV/R2 fixtures were refreshed with four clearly labeled synthetic earthquakes and one cluster. No production resource was seeded or migrated.
- The isolated code-only checkout passed 1,535 tests across 125 files, with three skips, and built successfully. Focused tests for the follow-up cluster timestamp correction passed. The initial preview version passed 117 GET checks, including 85 current or archived assets. The final preview version passed read-only checks of all four sitemap responses: canonical quake and cluster URLs, 13 static URLs, and no `<lastmod>` values. The full 117-GET smoke was not rerun after the sitemap-only follow-up.
- At the retention preflight, production still served source `65abd594a15fbc896b0a8e625ce01531a997908b`, Worker version `36303d5a-624f-4b4a-a997-dd8c2f165fa2`; these SEO fixes were **not yet live**. Search Console submission was deferred until the corrected release.

## Production release and Search Console submission

- The released source is `69a91d4b6f8d6512f0c5f99e3118835b33f8be75`, adding the verified predecessor asset graph to the SEO code. The full local suite passed **1,535 tests across 125 files**, with three existing skips. Preview version `9450284a-9abd-4b9c-811e-46da5ea0865e` passed **137 GET / 105 asset checks**. Both isolated preview and production R2 archives were verified at **85 assets / 15,272,299 bytes**.
- [Cloudflare automatic build 1d5efa2d](https://dash.cloudflare.com/f7e27d63f4766d7fb6a0f5b4789e2cdb/workers/services/view/earthquake/production/builds/1d5efa2d-d630-4cdb-ac5a-49fe0748099a) succeeded. GitHub CI [run 35818228321](https://github.com/watkajtys/earthquake/actions/runs/35818228321) and [run 35818228377](https://github.com/watkajtys/earthquake/actions/runs/35818228377) succeeded. Production Worker version `b70c02dd-24e0-4d25-ac1f-114844607862` served the exact source on both public hosts.
- Live readback found three sitemap index entries, 13 static URLs, 500 cluster URLs, and 12,067 URLs in earthquake sitemap page 1. Sampled entity URLs used canonical paths and the sitemaps omitted unsupported `<lastmod>` values. The first emitted cluster URL resolved, and `robots.txt` pointed to the sitemap index. Google Search Console accepted `https://earthquakeslive.com/sitemap-index.xml`; its row showed **Success** and initially **0 discovered pages**. That initial count is a submission observation, not an indexing outcome.
- Cloudflare Cron events showed **Success** for the `*/5`, `*/10`, and `*/30` schedules at **04:30:33 UTC** after release. That table does not expose the Worker version, so these outcomes cannot be attributed to the exact released version.
- The cluster sitemap still caps output at 500 rows. Indexing changes, crawler request volume, and upstream traffic reduction remain unmeasured. The source-revision writer guard and historical data repair remain separate work.

## Read-only production database baseline

At the initial triage check, D1 held 190,102 earthquake rows, 10,906 with event time in the last 30 days, and 45,377 cluster rows. The database size reported by D1 was approximately 782 MB. These were a planning baseline, not a repair manifest. No production data changed during that initial triage. The later verified export, local restore rehearsal, and additive production schema migration are recorded in the source-revision remediation note; no historical deduplication was performed.
